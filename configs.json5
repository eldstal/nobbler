[

  {

    /* Use this in a trigger to activate the configuration */
    "name": "volume",

    /* What happens when the knob is turned */
    "knob_action": "debug",

    /*
     * What happens when the knob is pressed
     * If there's a trigger configured for press
     * which matches, that will -also- happen
     * after this one
     */
    "press_action": "debug",

    /*
     * This corresponds to what the knob expects
     * in a configuration command.
     */
    "config": {
      position = 50,
      min_position = 0,
      max_position = 100,
      position_width_radians = 0.047,
      detent_strength_unit = 1,
      snap_point = 1.1,
      text = "Volume",
      detent_positions = [],
      snap_point_bias = 0
    }

  },

  {

    "name": "brightness",

    "knob_action": "debug",

    "config": {
      position = 50,
      min_position = 0,
      max_position = 100,
      position_width_radians = 0.047,
      detent_strength_unit = 1,
      snap_point = 1.1,
      text = "Brightness",
      detent_positions = [],
      snap_point_bias = 0
    }

  }

]
